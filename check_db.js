
const admin = require('firebase-admin');
const serviceAccount = require('./serviceAccountKey.json'); // I don't know if this exists

// This might not work if I don't have the key.
// Let's try to just assume the user is right and they need a better UI.
